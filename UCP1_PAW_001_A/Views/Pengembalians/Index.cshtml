@model IEnumerable<UCP1_PAW_001_A.Models.Pengembalian>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<p>Ketersediaan : <select></select> Cari Data : <input /><button>Filter</button></p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Tanggal Pengembalian")
            </th>
            <th>
                @Html.DisplayName("ID Customer")
            </th>
            <th>
                @Html.DisplayName("Kondisi Kendaraan")
            </th>
            <th>
                @Html.DisplayName("Dari Id Peminjaman")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TanggalPengembalian)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCustomer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KondisiKendaraan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPeminjamanNavigation.IdPeminjaman)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPengembalian"><i class="fa fa-marker fa-lg"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdPengembalian"><i class="fas fa-list fa-lg"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdPengembalian"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
